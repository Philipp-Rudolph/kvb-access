services:
  app:
    build:
      context: .
    volumes:
      - .:/app # Mount the entire repo (or just src/ if needed)
      - /app/node_modules # Use an isolated node_modules in the container
    ports:
      - '8080:8080'
    command: ['npm', 'run', 'dev']
